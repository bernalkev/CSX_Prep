/* Countdown
Create a recursive function countdown that accepts a positive integer n as an input 
and logs every number from n (inclusive) to 0 (exclusive) to the console.
*/




// function sum(array, sume = 0, index = 0){
//   if(array[index] === undefined) return sume;
//   return sum(array, sume + array[index], index + 1) 
// }


// const sum = (numbers, index = 0) => {
//   if (array[index] === undefined) return 0;
//   return array[index] + sum(numbers, index + 1);
// }


// console.log(sum([2, 2, 4, 7]))

//http://csbin.io/recursion

//https://www.w3resource.com/javascript-exercises/javascript-recursion-functions-exercises.php